Shadow `ra-core` for future extencility or replacement of replacement of
`ra-core` with a custom implementation. This allows us to have more control over
the core logic and potentially add features that are not available in `ra-core`.