# Strato Admin Examples

This directory contains example applications showcasing the use of `strato-admin` with various features.

## Examples

- [demo](./demo): A basic example listing, details, create and edit views.

## How to run

From each example directory, run the following commands to install dependencies and start the development server:

```bash
pnpm install
pnpm run dev
```
